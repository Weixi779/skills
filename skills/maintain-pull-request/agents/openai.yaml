interface:
  display_name: "Maintain Pull Request"
  short_description: "Review and safely maintain existing GitHub PRs"
  default_prompt: "Use $maintain-pull-request to inspect and handle the requested work on my existing pull request."
