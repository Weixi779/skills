interface:
  display_name: "Create Pull Request"
  short_description: "Safely publish changes as a draft GitHub PR"
  default_prompt: "Use $create-pull-request to publish my current changes as a draft pull request to the base branch I specify."
